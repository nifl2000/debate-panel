{
  "name": "debate-panel",
  "private": true,
  "workspaces": [
    "frontend",
    "workers"
  ],
  "scripts": {
    "dev": "concurrently --kill-others \"npm run dev:workers\" \"npm run dev:frontend\"",
    "dev:workers": "cd workers && wrangler dev",
    "dev:frontend": "cd frontend && VITE_API_BASE_URL=http://localhost:8787 npm run dev",
    "build": "cd workers && npm run typecheck",
    "test": "npm run test:backend && npm run test:workers && npm run test:frontend",
    "test:backend": "cd backend && pytest -v --tb=short",
    "test:workers": "cd workers && npm test",
    "test:frontend": "cd frontend && npm test -- --run",
    "deploy": "npm run deploy:workers && npm run deploy:frontend",
    "deploy:workers": "cd workers && wrangler deploy",
    "deploy:frontend": "cd frontend && wrangler pages deploy dist --project-name=debate-panel",
    "typecheck": "cd workers && npm run typecheck && cd ../frontend && npx tsc --noEmit"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
